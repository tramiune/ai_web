# Copy từ DevTools → Network → jobs → Request Headers → Cookie
# Chỉ cần JSESSIONID là đủ (hoặc full cookie string)
AIDANCING_COOKIE=JSESSIONID=your_session_here

# Poll interval khi monitor (giây) — tùy chọn, bot.py dùng BOT_POLL_* hiện có
# BOT_POLL_WAIT_RENDER_SEC=120
# BOT_POLL_ACTIVE_SEC=90
