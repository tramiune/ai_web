/**
 * Ads / pixel IDs — paste from TikTok Events Manager & Meta Events Manager.
 * Meta: business.facebook.com → Events Manager → Data sources → your Pixel ID (numeric).
 */
window.ANALYTICS_CONFIG = {
    metaPixelId: '1658541491887728'
};
